{% extends 'template.html.twig' %}
{% block title %}
    Editer une personne
{% endblock %}

{% block body %}
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}})}}
    <div class="row">
        <div class="col">{{ form_row(form.fisrtname) }}</div>
        <div class="col">{{ form_row(form.name) }}</div>
    </div>
    {{ form_rest(form) }}
    <script>
        $(document).ready(function() {
            $('.select2').select2();
        });
    </script>
{% endblock %}




